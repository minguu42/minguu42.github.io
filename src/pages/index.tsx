import React from "react";

import s from "@/src/styles/Home.module.css";

const Home: React.FC = () => {
  return (
    <main className={s.main}>
      <h1>Hello, World!</h1>
    </main>
  );
};

export default Home;
